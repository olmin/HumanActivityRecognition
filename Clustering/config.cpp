#include "config.h"
//external variables
//to be used in execution time if we want to change it
int numClusters;

//to be used in execution time if we want to change it
int numStates;

//to be used in execution time if we want to change it
int compressionRate;

//to be used in execution time if we want to change it
int kRate;

//to be used in execution time if we want to change it
int actualIdUser;

//external variables

 Driver *driver;
 Connection *con;
 Savepoint *savept;